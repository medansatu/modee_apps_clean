class Profile {
  final int id;
  final String? name, userName, email, address, phoneNumber, imageUrl;

Profile({
  required this.id, required this.name, required this.userName, required this.email, required this.phoneNumber, required this.address, this.imageUrl,
});
}
